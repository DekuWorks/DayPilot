import { ComingSoon } from "@/components/ComingSoon";

export default function MeetingsPage() {
  return (
    <ComingSoon
      title="Meetings"
      description="Upcoming and past meetings with join links, attendees, and notes."
    />
  );
}
