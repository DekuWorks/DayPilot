import { ComingSoon } from "@/components/ComingSoon";

export default function NotesPage() {
  return (
    <ComingSoon
      title="Notes"
      description="Markdown notes linked to events, projects, and meetings."
    />
  );
}
