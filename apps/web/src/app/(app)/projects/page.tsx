import { ComingSoon } from "@/components/ComingSoon";

export default function ProjectsPage() {
  return (
    <ComingSoon
      title="Projects"
      description="Lightweight projects with tasks, notes, and progress — coming in the web core phase."
    />
  );
}
