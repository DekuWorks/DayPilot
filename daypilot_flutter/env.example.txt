# Copy values into your flutter run command or VS Code launch.json.
# Flutter does not load this file automatically.

# Supabase (Project Settings → API)
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=your_anon_key

# Nest API base URL, no trailing slash.
# Local: http://localhost:3001
# Android emulator → host machine: http://10.0.2.2:3001
# Production: https://api.yourdomain.com
DAYPILOT_API_URL=http://localhost:3001

# API server only (not in Flutter):
# SUPABASE_JWT_SECRET=<same as Supabase JWT Secret in dashboard>
